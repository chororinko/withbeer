<div class="container mt-4 ml-10">
  <div class="col-2 text-center" style="font-family: 'Playfair Display', serif; background-color:#faf0e6;">
    <h4>お酒ジャンル編集</h4>
  </div>
</div>

<%= form_with model: @sake, url: admin_sake_path(@sake), method: :patch do |f| %>
<div class="container mt-5 ml-5">
  <div class="row d-flex justify-content-center">
    <div class="col-2 d-flex justify-content-center">
      <h6>ジャンル名</h6>
    </div>

    <div class="col-2">
      <%= f.text_field :genre %>
    </div>

    <div class="col-2 d-flex justify-content-end">
      <%= f.submit '変更を保存', class: 'btn btn-info btn-lg' %>
    </div>
    <!--<div class="col-2 d-flex justify-content-end">-->
    <!--  <#%= f.submit '削除', admin_sake_path(@sake), method: :delete, class: 'btn btn-danger btn-lg' %>-->
    <!--</div>-->
  </div>
</div>
<% end %>
